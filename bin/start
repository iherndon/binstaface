#!/usr/bin/env node
const binstaface = require('../lib');
const path = require('path');

binstaface(path.join(__dirname, '..')).listen(1337, function() {
  console.log(`Server listening on port 1337`);
});